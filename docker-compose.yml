services:
  mddp-app:
    build: .
    container_name: mddp_app
    restart: unless-stopped
    # Privileged mode, host device tree & Advantech driver shared libraries (Linux Host)
    privileged: true
    volumes:
      - /dev:/dev
      - /usr/lib:/host_usr_lib:ro
      - /usr/local/lib:/host_usr_local_lib:ro
      - /etc/biobdaq:/etc/biobdaq:ro
    environment:
      - LD_LIBRARY_PATH=/host_usr_lib:/host_usr_local_lib:/usr/lib:/usr/local/lib
    # Device mapping for physical RS-232 / USB serial dispenser controllers (e.g. Musashi)
    # devices:
    #   - "/dev/ttyUSB0:/dev/ttyUSB0"
    #   - "/dev/ttyACM0:/dev/ttyACM0"
    ports:
      - "8080:8080" # Portal Gateway
      - "8081:8081" # DAQ Control Panel
      - "8083:8083" # Musashi IV Panel
      - "8084:8084" # Database Plotter Service

  # Optional Test Plugins (TimescaleDB & Mosquitto MQTT Broker)
  # Uncomment the services below if running local database / broker containers
  #
  # timescaledb:
  #   image: timescale/timescaledb:latest-pg16
  #   container_name: daq_tsdb
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: admin
  #     POSTGRES_DB: daq_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - ./pgdata:/var/lib/postgresql/data
  #     - ./db_setup.sql:/docker-entrypoint-initdb.d/init.sql
  #
  # mosquitto:
  #   image: eclipse-mosquitto:latest
  #   container_name: daq_mqtt
  #   restart: unless-stopped
  #   ports:
  #     - "1883:1883"
  #     - "9001:9001"
  #   command: sh -c 'echo "listener 1883\nallow_anonymous true" > /mosquitto/config/mosquitto.conf && /usr/sbin/mosquitto -c /mosquitto/config/mosquitto.conf'
